{
  "name": "tickerdb-mcp",
  "version": "1.7.9",
  "private": true,
  "description": "Pre-computed EOD market context that improves reasoning, reduces token usage, and replaces data pipelines.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc -p tsconfig.json",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.5",
    "wrangler": "^4.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250310.0",
    "typescript": "^5.7.0"
  }
}
