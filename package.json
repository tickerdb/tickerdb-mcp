{
  "name": "tickerapi-mcp",
  "private": true,
  "workspaces": [
    "shared",
    "remote",
    "local"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:shared": "npm run build -w shared",
    "build:remote": "npm run build -w remote",
    "build:local": "npm run build -w local"
  }
}